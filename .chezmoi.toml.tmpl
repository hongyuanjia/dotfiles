{{- if lookPath "delta" -}}
[diff]
    command = "delta"
    pager = "delta"
{{- end }}
encryption = "age"
[age]
    identity = "~/.ssh/id_rsa"
    symmetric = true
[merge]
    command = "nvim"
    args = ["-d", "{{ .Destination }}", "{{ .Source }}", "{{ .Target }}"]
